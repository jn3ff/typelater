[package]
name = "typelater"
version = "0.1.0"
edition = "2024"

[dependencies]
typelater-derive = { path = "../typelater-derive"}

[lib]
proc-macro = false

[dev-dependencies]
chrono = { version = "0.4.42", features = ["serde"]}
